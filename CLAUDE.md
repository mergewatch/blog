# Repository instructions

Follow `AGENTS.md`. In particular, inspect the current MergeWatch product repository before altering brand treatment; keep production canonicals on `https://mergewatch.ai/blog`; fail every non-production environment closed to `noindex, nofollow`; never publish drafts; avoid runtime infrastructure and unnecessary dependencies; and do not couple this official content site to customer-hosted MergeWatch installations.
